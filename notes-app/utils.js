/** The following file is a dummy file. This is not required. */
console.log('Running utils.js...')

const name = 'Rambo';

const add = function (a, b) {
    return a + b;
}

// module.exports = name;
module.exports = add;
